# One extended regex per line; '#' lines and blanks ignored.
# THE single source for secret-scan patterns — consumed by .githooks/pre-commit AND
# .github/workflows/lints.yml via build/scripts/secret-scan.mjs. Never inline these
# patterns in a scannable file: the scanner excludes THIS file from the scanned diff
# (a regex is not a secret), which is what makes the pattern list itself committable.
# This file is review-sensitive: a real secret hidden here would be exempt; treat any
# change to it as a security-relevant diff.
AKIA[0-9A-Z]{16}
-----BEGIN [A-Z ]*PRIVATE KEY-----
sk_live_[0-9a-zA-Z]{20,}
DefaultEndpointsProtocol=https;AccountName=
