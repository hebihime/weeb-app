# Features/Inbox

Deliberately empty at S7 (SLICE_S7_CONTRACT.md §1b: "Other Features/* dirs exist but empty"). This
directory reserves the layer name so a future slice mounts real content here as an adapter registration,
never shell surgery. AppShell renders this tab's honest zero-data state directly at S7 (see
ios/AppShell/Sources/AppShell/Tabs.swift) since there is nothing else to compose yet.
