# ---- OS / editor junk ----
.DS_Store
*.swp
*~
.idea/
.vscode/

# ---- Claude Code / gstack local state ----
.gstack/
.claude/settings.local.json

# ---- Design upload staging (zip is a binary artifact) ----
claude-design-upload-weebapp.zip

# ---- Secrets / env ----
.env
.env.*
!.env.example
*.pem
*.key
*.p8
*.p12
*.jks
*.mobileprovision
secrets.json
appsettings.*.Local.json
fastlane/.env*
fastlane/report.xml
match-*.git-crypt

# ---- .NET (backend) ----
bin/
obj/
# NOTE: do NOT add unanchored `[Dd]ebug/` / `[Rr]elease/` here. .NET build output already lives under
# bin/ + obj/ (covered above); an unanchored debug/release dir pattern also silently swallows SOURCE
# directories like android/app/src/debug/ (S7: the whole Android debug source set went uncommitted this
# way, absent on CI). If a non-standard .NET output dir ever appears, anchor it (e.g. backend/**/bin/).
*.user
project.lock.json
.vs/
TestResults/

# ---- Node / web funnel ----
node_modules/
dist/
build/
# /build at repo root holds build-kit scripts (workflow templates), not build output
!/build/
.next/
.turbo/
*.log
npm-debug.log*
yarn-error.log*
.pnp.*

# ---- iOS (Swift) ----
xcuserdata/
*.xcuserstate
DerivedData/
.build/
Pods/
*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings

# ---- Android (Kotlin) ----
*.iml
.gradle/
local.properties
android/build/
android/app/build/
captures/
*.apk
*.aab
*.keystore

# ---- Infra ----
*.tfstate
*.tfstate.*
.terraform/
